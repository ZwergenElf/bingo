events {}
http {
    include s/etc/nginx/mime.types;

    server {
        listen 8080;
        root /usr/share/nginx/html;
        index index.html;
        server_name zwergen.site www.zwergen.site;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}